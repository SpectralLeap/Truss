name: PR Target Checker

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  check-pr-target:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
